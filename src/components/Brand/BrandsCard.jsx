/* eslint-disable react/prop-types */
const BrandsCard = ({ coupon }) => {
  const {
    brand_name,
    rating,
    description,
    brand_logo,
    coupons,
    shop_Link,
    category,
    isSaleOn,
  } = coupon;
  return (
    <div>
      
    </div>
  );
};

export default BrandsCard;
